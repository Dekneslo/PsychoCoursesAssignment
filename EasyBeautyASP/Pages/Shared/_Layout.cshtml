<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Психология Онлайн</title>

    <!-- Стили -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="@Url.Content("~/assets/css/style.css")">
    <link rel="stylesheet" type="text/css" href="@Url.Content("~/assets/css/form.css")">
    <link rel="icon" type="image/x-icon" href="@Url.Content("~/assets/media/icon.ico")">

    <style>
        #mySidebar {
            height: 300px;
            width: 0;
            position: fixed;
            z-index: 1050;
            top: 122px;
            right: 0;
            background-color: #f9f9f9;
            overflow-x: hidden;
            transition: 0.5s;
            box-shadow: -2px 0 5px rgba(0, 0, 0, 0.1);
            border-left: 1px solid #ccc;
        }

        #mySidebar .closebtn {
            position: absolute;
            top: 10px;
            left: 10px;
            font-size: 30px;
            font-weight: bold;
            color: #333;
            z-index: 9999;
        }

        #mySidebar .dropdown {
            margin-top: 50px;
        }

        #mySidebar a {
            font-size: 16px;
            text-decoration: none;
        }
    </style>
</head>

<body>

    <!-- Хедер -->
    <header class="header" id="heading">
        <nav class="navbar navbar-expand-lg sticky-top">
            <div class="container-fluid">
                <a class="navbar-brand" asp-page="Index">
                    <img src="@Url.Content("~/assets/media/logo.png")" class="logo">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                        aria-controls="navbarNav" aria-expanded="false" aria-label="Переключить навигацию">
                    <span class="navbar-toggler-icon icon-style"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav navbar-style ms-auto pe-5 mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link" asp-page="Index" id="active">Главная</a></li>
                        <li class="nav-item"><a class="nav-link" asp-page="About">О нас</a></li>
                        <li class="nav-item"><a class="nav-link" asp-page="Shop">Курсы</a></li>
                        <li class="nav-item"><a class="nav-link" asp-page="Blog">Блог</a></li>
                        <li class="nav-item"><a class="nav-link" asp-page="Contact">Контакты</a></li>
                        <li class="nav-item"><partial name="_LoginPartial" /></li>
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item"><a class="nav-link" asp-page="ShopAdmin">Админ</a></li>
                        }
                        @if (User.IsInRole("Customer") || User.IsInRole("Admin"))
                        {
                            <li class="nav-item"><a class="nav-link" asp-page="Cart">Корзина</a></li>
                        }
                        <li class="nav-item">
                            <a class="nav-link" href="#" onclick="toggleSidebar()">
                                Настройки <i class="fa fa-sliders" aria-hidden="true"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- Контент -->
    <section>
        @RenderBody()
    </section>


    <!-- Футер -->
    <footer class="text-center text-lg-start text-muted footer-style mt-5">
        <section class="d-flex justify-content-center p-4 border-bottom">
            <div class="me-5 d-none d-lg-block content">
                <span>Связаться с нами в соцсетях:</span>
            </div>
            <div>
                <a href="#" class="me-4 link-secondary">
                    <i class="fa fa-facebook-f"></i>
                </a>
                <a href="#" class="me-4 link-secondary">
                    <i class="fa fa-twitter"></i>
                </a>
                <a href="#" class="me-4 link-secondary">
                    <i class="fa fa-google"></i>
                </a>
                <a href="#" class="me-4 link-secondary">
                    <i class="fa fa-instagram"></i>
                </a>
                <a href="#" class="me-4 link-secondary">
                    <i class="fa fa-linkedin"></i>
                </a>
                <a href="#" class="me-4 link-secondary">
                    <i class="fa fa-github"></i>
                </a>
            </div>
        </section>

        <section class="">
            <div class="container text-center text-md-start mt-5">
                <div class="row mt-3">
                    <div class="col-md-3 col-lg-4 col-xl-3 mx-auto mb-4">
                        <h6 class="text-uppercase fw-bold mb-4">
                            <img src="assets/media/logo.png" alt="Психология Онлайн" width="100%">
                        </h6>
                        <p>Платформа для изучения психологии и саморазвития онлайн.</p>
                    </div>

                    <div class="col-md-2 col-lg-2 col-xl-2 mx-auto mb-4">
                        <h6 class="text-uppercase fw-bold mb-4">
                            Курсы
                        </h6>
                        <p><a href="#" class="text-reset">Психология</a></p>
                        <p><a href="#" class="text-reset">Саморазвитие</a></p>
                        <p><a href="#" class="text-reset">Обучение</a></p>
                        <p><a href="#" class="text-reset">Тренинги</a></p>
                    </div>

                    <div class="col-md-3 col-lg-2 col-xl-2 mx-auto mb-4">
                        <h6 class="text-uppercase fw-bold mb-4">
                            Меню
                        </h6>
                        <p><a href="#" class="text-reset">Акции</a></p>
                        <p><a href="#" class="text-reset">Магазин</a></p>
                        <p><a href="#" class="text-reset">Цены</a></p>
                        <p><a href="#" class="text-reset">Политика</a></p>
                    </div>

                    <div class="col-md-4 col-lg-3 col-xl-3 mx-auto mb-md-0 mb-4">
                        <h6 class="text-uppercase fw-bold mb-4">Контакты</h6>
                        <p><i class="fa fa-home me-3 text-secondary"></i> UK, Ноттингем</p>
                        <p><i class="fa fa-envelope me-3 text-secondary"></i> support@psyonline.com</p>
                        <p><i class="fa fa-phone me-3 text-secondary"></i> +44 1234 567890</p>
                    </div>
                </div>
            </div>
        </section>
    </footer>


    <!-- Сайдбар -->
    <div id="mySidebar">
        <a href="javascript:void(0)" class="closebtn" onclick="closeSidebar()">&times;</a>

        <div class="dropdown mb-4">
            <a class="btn btn-secondary dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">Размер шрифта</a>
            <div class="dropdown-menu">
                <a class="dropdown-item font-button plus" href="#">Увеличить</a>
                <a class="dropdown-item font-button minus" href="#">Уменьшить</a>
            </div>
        </div>

        <div class="dropdown">
            <a class="btn btn-secondary dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">Фон шапки</a>
            <div class="dropdown-menu">
                <a class="dropdown-item" href="#" onclick="changeHeadingBg('aliceblue')">Алисовый</a>
                <a class="dropdown-item" href="#" onclick="changeHeadingBg('antiquewhite')">Античный</a>
                <a class="dropdown-item" href="#" onclick="changeHeadingBg('aquamarine')">Аквамарин</a>
            </div>
        </div>
    </div>


    <!-- Скрипты -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

    <!-- JQuery Validate -->
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js"></script>

    <!-- JQuery Unobtrusive Validate -->
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation-unobtrusive@3.2.12/dist/jquery.validate.unobtrusive.min.js"></script>

    <!-- Sidebar и другие JS -->
    <script>
        let sidebarOpen = false;

        function toggleSidebar() {
            const sidebar = document.getElementById("mySidebar");
            sidebar.style.width = sidebarOpen ? "0" : "250px";
            sidebarOpen = !sidebarOpen;
        }

        function closeSidebar() {
            document.getElementById("mySidebar").style.width = "0";
            sidebarOpen = false;
        }

        function changeHeadingBg(color) {
            document.getElementById('heading').style.backgroundColor = color;
        }

        $(document).ready(function () {
            $(".font-button").on("click", function () {
                let size = parseInt($("html").css("font-size"));
                size = $(this).hasClass("plus") ? size + 2 : Math.max(10, size - 2);
                $("html").css("font-size", size + "px");
            });

            $('.dropdown-menu').on("click.bs.dropdown", function (e) {
                e.stopPropagation();
            });
        });
    </script>

    @RenderSection("Scripts", required: false)



</body>

</html>
